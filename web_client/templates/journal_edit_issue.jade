#headerBar
.Wrapper
  .Content
    div
      br
      form#issueForm.mainForm(data-validate="parsley", method="POST", action="")
        table(width="100%", border="0")
          tbody
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong Name:
              td(width="75%")
                input#issueName.parsley-validated(type="text", value=info.name, required, name="name")
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong Description:
              td(width="75%")
                textarea#issueDescription(name="short_description", required)
                  | #{info.description}
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong Related Links:
              td(width="75%")
                textarea(name="related_link")
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong License (Publisher):
              td(width="75%")
                |Set license to
                select (name="licensemenu", onchange="ChangeAuthorLicense(this.value)")
                br
                |or modify the following text
                br
                textarea#publishLicense(name="publishLicense")
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong License (Author):
              td(width="75%")
                |Set license to
                select (name="copyrightmenu", onchange="ChangeReaderLicense(this.value)")
                br
                |or modify the following text
                br
                textarea#authorLicense(name="authorLicense")
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong Paperdue date:
              td
                - var paperDue=''
                if info.meta
                  - paperDue=info.meta.paperDue
                input.datepicker.picker__input.parsley-validated(type="date", data-type="dateIso", required, name="paperdue_date", value=paperDue, placeholder="Select a date.")
                input(type="hidden", name="paperdue_date_submit")
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong Decision date:
              td
                - var decision=''
                if info.meta
                  - decision=info.meta.decision
                input.datepicker.picker__input(type="date", data-type="dateIso", required, value=decision, name="decision_date")
                input(type="hidden", name="decision_date_submit")
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong Publication date
              td
                - var publication=''
                if info.meta
                  - publication=info.meta.publication
                input.datepicker.picker__input.parsley-validated(type="date", data-type="dateIso", required, name="publication_date", value=publication, placeholder="Select a date.")
                input(type="hidden", name="publication_date_submit")
            tr
              td(width="25%", style="vertical-align: top;")
                div(align="right")
                  strong Private:
              td
                input#privateIssue(type="checkbox", value="1", name="defaultpolicy")
            tr
              td
              td
                input(type="submit", value=" << Cancel", name="data[Admin][cancel]")
                input(type="submit", value="Create >>", name="data[Admin][add]")
